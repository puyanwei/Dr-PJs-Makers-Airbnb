<!--includes the partials, so all you need to write is the body!-->
<% include partials/header %>

<h1>Welcome to Dr. PJs MakersBnB!</h1>

<% if(errors != undefined){ %>
<ul>
    <% errors.forEach(function(error){ %>
    <li>
        <%= error.msg %>
    </li>
    <% })%>
</ul>
<% } %>

<h2>Rooms</h2>

<ul>
    <% rooms.forEach(function(room) { %>
    <li>
        <%= room.title %>,
        <%= room.owner %>,
        <%= room.location %>,
        <%= room.description %>,
        £<%= room.price %>
    </li>
    <% })%>
</ul>

<% include partials/footer %>
