<% include partials/header %>

<h1> <%= thing %></h1>

<li class="blueClass">
    <%= room.title %>,
    <%= room.owner %>,
    <%= room.location %>,
    <%= room.description %>,
    £<%= room.price %>
</li>

<form method="POST" action="/rooms/confirm">
    <button type="submit" name="bookRoomName" value="<%= room.title %>">Book This Room</button>
</form>

<form method="GET" action="/rooms">
    <button type="submit" name="viewRooms">View Rooms</button>
</form>

<% include partials/footer %>
